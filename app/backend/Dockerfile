FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py .
CMD ["opentelemetry-instrument" , "python" , "app.py"]


# Wrap the python execution with the OTel agent
#CMD ["opentelemetry-instrument", "python", "app.py"]
